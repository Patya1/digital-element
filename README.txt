Тестовое задание цифровой Элемент. 

Запуск проекта

cd Папка с проектом
npm install
npm run dev
npm run build
npm run lint

Время работы 4 дня (по несколько часов в день).
-Настройка проекта - Часа 4 (С учётом возникших проблем).
-Вёрстка и стилизация - Часов 5 (Практически без проблем).
-Работа с JS - Около 5 часов (С учётом ошибок и проблем).
-Eslint - 2-3 часа, чтобы поправить ошибки

Проблемы:
1. Настройка проекта.
    -npm init и package.json. Сначала не понимал что писать в package.json. Запустил npm init -y, он создал файл с настройками
    по умолчанию, но там не было секции scripts. Пришлось разбираться и добавлять команды dev, build и lint.
    -Установка зависимостей.Не знал какие пакеты ставить. Погуглил, понял , что нужен vite как сборщик,
    sass для компиляции SCSS и eslint для проверки кода. Разбирался как с ними работать.
2. Работа с JS.
    -Были проьдемы с валидацией формы.
    -Не знал как проверить что формаотправляет данные, нашёл публичный сервер https://jsonplaceholder.typicode.com/posts. 
    Его и использовал для проверки
3. Линтирование.
    -Раньше никогда не раотал ни с чем подобным, тоже ушло время чтобы разобраться.
    -Ушло пару часов чтобы исправить ошибки, которые он находил.
